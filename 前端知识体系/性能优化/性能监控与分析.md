# 📈 Web 性能监控与分析

> 性能优化的第一步：测量与监控

---

## 📚 目录

1. [性能指标](#性能指标)
2. [Performance API](#performance-api)
3. [Web Vitals 核心指标](#web-vitals-核心指标)
4. [Lighthouse 性能评分](#lighthouse-性能评分)
5. [真实用户监控 (RUM)](#真实用户监控-rum)
6. [性能分析工具](#性能分析工具)

---

## 性能指标

### ⏱️ 关键时间节点

```
导航开始 → DNS解析 → TCP连接 → 请求发送 → 响应接收 → DOM解析 → 页面渲染 → 可交互

|---------|---------|----------|-----------|------------|---------|------------|---------|
navigationStart  domainLookupStart  requestStart  responseStart  domLoading  domInteractive  loadEventEnd
```

---

### 📊 核心指标

| 指标 | 说明 | 目标值 |
|:---:|:---|:---:|
| **FP** | First Paint - 首次绘制 | < 1s |
| **FCP** | First Contentful Paint - 首次内容绘制 | < 1.8s |
| **LCP** | Largest Contentful Paint - 最大内容绘制 | < 2.5s |
| **FID** | First Input Delay - 首次输入延迟 | < 100ms |
| **TTI** | Time to Interactive - 可交互时间 | < 3.8s |
| **CLS** | Cumulative Layout Shift - 累积布局偏移 | < 0.1 |
| **TTFB** | Time to First Byte - 首字节时间 | < 600ms |

---

## Performance API

### 🔧 使用 Navigation Timing API

```javascript
// 获取性能数据
const perfData = performance.getEntriesByType('navigation')[0]

// 关键时间点
const timing = {
  // DNS 解析时间
  dns: perfData.domainLookupEnd - perfData.domainLookupStart,
  
  // TCP 连接时间
  tcp: perfData.connectEnd - perfData.connectStart,
  
  // SSL 握手时间
  ssl: perfData.secureConnectionStart > 0 
    ? perfData.connectEnd - perfData.secureConnectionStart 
    : 0,
  
  // TTFB（首字节时间）
  ttfb: perfData.responseStart - perfData.requestStart,
  
  // 响应下载时间
  download: perfData.responseEnd - perfData.responseStart,
  
  // DOM 解析时间
  domParse: perfData.domInteractive - perfData.domLoading,
  
  // 资源加载时间
  resourceLoad: perfData.loadEventStart - perfData.domContentLoadedEventEnd,
  
  // 总时间
  total: perfData.loadEventEnd - perfData.fetchStart
}

console.table(timing)
```

---

### 📦 监控资源加载

```javascript
// 监控所有资源加载
const resources = performance.getEntriesByType('resource')

resources.forEach(resource => {
  console.log({
    name: resource.name,
    type: resource.initiatorType,  // script, css, img, etc.
    duration: resource.duration,
    size: resource.transferSize,   // 传输大小
    cached: resource.transferSize === 0  // 是否使用缓存
  })
})

// 找出加载最慢的资源
const slowestResources = resources
  .sort((a, b) => b.duration - a.duration)
  .slice(0, 10)

console.table(slowestResources)
```

---

### 🎯 自定义性能标记

```javascript
// 标记开始
performance.mark('task-start')

// 执行任务
await fetchData()

// 标记结束
performance.mark('task-end')

// 计算耗时
performance.measure('task-duration', 'task-start', 'task-end')

// 获取结果
const measure = performance.getEntriesByName('task-duration')[0]
console.log(`任务耗时: ${measure.duration}ms`)
```

---

### 💡 Vue 组件性能监控

```vue
<script setup>
import { onMounted } from 'vue'

// 组件挂载性能监控
performance.mark('component-mount-start')

onMounted(() => {
  performance.mark('component-mount-end')
  performance.measure(
    'component-mount',
    'component-mount-start',
    'component-mount-end'
  )
  
  const measure = performance.getEntriesByName('component-mount')[0]
  console.log(`组件挂载耗时: ${measure.duration}ms`)
})
</script>
```

---

## Web Vitals 核心指标

### 🎯 Google 核心 Web Vitals

```
用户体验的三大核心指标：

LCP (加载性能)  → 页面主要内容多久加载完成？
FID (交互性)    → 用户点击后多久响应？
CLS (视觉稳定)  → 页面布局是否稳定？
```

---

### 1️⃣ LCP - Largest Contentful Paint

**最大内容绘制时间**：页面主要内容加载完成的时间

```javascript
import { onLCP } from 'web-vitals'

onLCP((metric) => {
  console.log('LCP:', metric.value, 'ms')
  
  // 上报数据
  sendToAnalytics({
    name: 'LCP',
    value: metric.value,
    rating: metric.rating  // 'good' | 'needs-improvement' | 'poor'
  })
})
```

**评分标准：**
- ✅ 优秀: < 2.5s
- ⚠️ 需要改进: 2.5s - 4.0s
- ❌ 差: > 4.0s

**如何优化：**
```
✅ 优化图片加载（压缩、WebP、懒加载）
✅ 减少阻塞渲染的资源
✅ 服务端渲染（SSR）
✅ 使用 CDN 加速
```

---

### 2️⃣ FID - First Input Delay

**首次输入延迟**：用户首次交互到浏览器响应的时间

```javascript
import { onFID } from 'web-vitals'

onFID((metric) => {
  console.log('FID:', metric.value, 'ms')
  
  sendToAnalytics({
    name: 'FID',
    value: metric.value,
    rating: metric.rating
  })
})
```

**评分标准：**
- ✅ 优秀: < 100ms
- ⚠️ 需要改进: 100ms - 300ms
- ❌ 差: > 300ms

**如何优化：**
```
✅ 减少 JavaScript 执行时间
✅ 代码分割，按需加载
✅ 使用 Web Worker
✅ 优化第三方脚本
```

---

### 3️⃣ CLS - Cumulative Layout Shift

**累积布局偏移**：页面元素意外移动的程度

```javascript
import { onCLS } from 'web-vitals'

onCLS((metric) => {
  console.log('CLS:', metric.value)
  
  sendToAnalytics({
    name: 'CLS',
    value: metric.value,
    rating: metric.rating
  })
})
```

**评分标准：**
- ✅ 优秀: < 0.1
- ⚠️ 需要改进: 0.1 - 0.25
- ❌ 差: > 0.25

**如何优化：**
```
✅ 为图片和视频指定宽高
✅ 避免在现有内容上方插入内容
✅ 使用 transform 动画代替会改变布局的属性
✅ 预留广告位空间
```

---

### 📦 完整的 Web Vitals 监控

```javascript
// 安装
npm install web-vitals

// 监控所有指标
import { onCLS, onFID, onFCP, onLCP, onTTFB } from 'web-vitals'

function sendToAnalytics(metric) {
  // 上报到分析平台
  const body = JSON.stringify({
    name: metric.name,
    value: metric.value,
    rating: metric.rating,
    delta: metric.delta,
    id: metric.id,
    url: window.location.href,
    userAgent: navigator.userAgent
  })
  
  // 使用 sendBeacon API（页面卸载时也能发送）
  navigator.sendBeacon('/api/analytics', body)
}

// 监控所有指标
onCLS(sendToAnalytics)
onFID(sendToAnalytics)
onFCP(sendToAnalytics)
onLCP(sendToAnalytics)
onTTFB(sendToAnalytics)
```

---

## Lighthouse 性能评分

### 🔍 什么是 Lighthouse

Google 开发的**开源自动化工具**，用于评估网页质量。

### 🚀 使用方式

#### 方法 1：Chrome DevTools

```
1. 打开 Chrome DevTools (F12)
2. 切换到 Lighthouse 标签
3. 选择类别（Performance、Accessibility、SEO 等）
4. 点击 "Analyze page load"
```

---

#### 方法 2：命令行

```bash
# 安装
npm install -g lighthouse

# 运行测试
lighthouse https://example.com --view

# 输出 JSON 报告
lighthouse https://example.com --output json --output-path report.json
```

---

#### 方法 3：CI/CD 集成

```yaml
# .github/workflows/lighthouse.yml
name: Lighthouse CI
on: [push]
jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Lighthouse
        uses: treosh/lighthouse-ci-action@v9
        with:
          urls: |
            https://example.com
            https://example.com/about
          budgetPath: ./budget.json
```

---

### 📊 评分标准

```
Performance 性能评分 (0-100)

90-100  ✅ 优秀 (绿色)
50-89   ⚠️ 需要改进 (橙色)
0-49    ❌ 差 (红色)
```

**评分权重：**

| 指标 | 权重 |
|:---:|:---:|
| FCP | 10% |
| SI (Speed Index) | 10% |
| LCP | 25% |
| TTI | 10% |
| TBT (Total Blocking Time) | 30% |
| CLS | 15% |

---

### 🎯 性能优化建议

Lighthouse 会给出具体建议：

```
机会 (Opportunities)：
✅ 压缩图片               → 节省 500KB
✅ 移除未使用的 CSS       → 节省 200KB
✅ 启用文本压缩           → 节省 300KB

诊断 (Diagnostics)：
⚠️ 主线程工作过多         → 减少 JavaScript 执行
⚠️ 避免大量的 DOM 节点   → 当前 2500 个，建议 < 1500
⚠️ 避免链式关键请求       → 减少依赖链深度
```

---

## 真实用户监控 (RUM)

### 🎯 什么是 RUM

**Real User Monitoring**：监控真实用户的性能数据，而不是实验室环境。

### 📊 RUM vs 实验室数据

| 特性 | 实验室数据 (Lighthouse) | RUM (真实用户) |
|:---:|:---:|:---:|
| **环境** | 模拟环境 | 真实环境 |
| **网络** | 固定 | 各种网络状况 |
| **设备** | 固定 | 各种设备 |
| **数据量** | 少 | 大 |
| **用途** | 开发调试 | 真实监控 |

---

### 🔧 实现 RUM 监控

```javascript
// utils/rum.js
class RUMMonitor {
  constructor() {
    this.metrics = {}
    this.init()
  }
  
  init() {
    // 监听页面加载完成
    window.addEventListener('load', () => {
      this.collectPerformance()
      this.reportMetrics()
    })
    
    // 监听 Web Vitals
    this.collectWebVitals()
    
    // 监听错误
    this.collectErrors()
  }
  
  // 收集性能数据
  collectPerformance() {
    const perfData = performance.getEntriesByType('navigation')[0]
    
    this.metrics.performance = {
      dns: perfData.domainLookupEnd - perfData.domainLookupStart,
      tcp: perfData.connectEnd - perfData.connectStart,
      ttfb: perfData.responseStart - perfData.requestStart,
      download: perfData.responseEnd - perfData.responseStart,
      domParse: perfData.domInteractive - perfData.domLoading,
      total: perfData.loadEventEnd - perfData.fetchStart
    }
  }
  
  // 收集 Web Vitals
  collectWebVitals() {
    import('web-vitals').then(({ onCLS, onFID, onLCP }) => {
      onLCP((metric) => this.metrics.lcp = metric.value)
      onFID((metric) => this.metrics.fid = metric.value)
      onCLS((metric) => this.metrics.cls = metric.value)
    })
  }
  
  // 收集错误
  collectErrors() {
    window.addEventListener('error', (event) => {
      this.reportError({
        type: 'js_error',
        message: event.message,
        filename: event.filename,
        lineno: event.lineno,
        colno: event.colno
      })
    })
    
    // 监听未处理的 Promise 错误
    window.addEventListener('unhandledrejection', (event) => {
      this.reportError({
        type: 'promise_error',
        message: event.reason
      })
    })
  }
  
  // 上报数据
  reportMetrics() {
    const data = {
      ...this.metrics,
      url: window.location.href,
      userAgent: navigator.userAgent,
      timestamp: Date.now(),
      // 用户信息（如果有）
      userId: this.getUserId()
    }
    
    // 使用 sendBeacon 上报
    navigator.sendBeacon('/api/rum', JSON.stringify(data))
  }
  
  reportError(error) {
    navigator.sendBeacon('/api/errors', JSON.stringify(error))
  }
  
  getUserId() {
    // 从 cookie 或 localStorage 获取用户 ID
    return localStorage.getItem('userId')
  }
}

// 启动监控
new RUMMonitor()
```

---

### 📊 数据分析维度

```javascript
// 按设备类型分析
{
  mobile: { lcp: 2800, fid: 150, cls: 0.12 },
  desktop: { lcp: 1500, fid: 80, cls: 0.05 },
  tablet: { lcp: 2000, fid: 100, cls: 0.08 }
}

// 按网络类型分析
{
  '4g': { lcp: 2000, fid: 100 },
  '3g': { lcp: 5000, fid: 300 },
  'wifi': { lcp: 1200, fid: 60 }
}

// 按地区分析
{
  'CN': { lcp: 2500, fid: 120 },
  'US': { lcp: 1800, fid: 90 }
}
```

---

## 性能分析工具

### 🛠️ 工具清单

#### 1. Chrome DevTools

```
Performance 面板：
- 记录页面加载过程
- 火焰图分析
- 查看 FPS、CPU、内存占用

Network 面板：
- 资源加载瀑布流
- 请求耗时分析
- 缓存使用情况

Coverage 面板：
- 代码覆盖率
- 查找未使用的代码
```

---

#### 2. webpack-bundle-analyzer

```bash
# 安装
npm install -D webpack-bundle-analyzer

# Vite 使用
npm install -D rollup-plugin-visualizer
```

```javascript
// vite.config.js
import { visualizer } from 'rollup-plugin-visualizer'

export default {
  plugins: [
    visualizer({
      open: true,  // 自动打开报告
      gzipSize: true,  // 显示 gzip 压缩后的大小
      brotliSize: true  // 显示 brotli 压缩后的大小
    })
  ]
}
```

---

#### 3. PageSpeed Insights

Google 提供的在线工具：https://pagespeed.web.dev/

```
功能：
✅ 实验室数据（Lighthouse）
✅ 真实用户数据（Chrome UX Report）
✅ 优化建议
✅ 桌面端 + 移动端评分
```

---

#### 4. WebPageTest

https://www.webpagetest.org/

```
功能：
✅ 全球多地点测试
✅ 多种设备和浏览器
✅ 详细的瀑布流图
✅ 视频录制
✅ 竞品对比
```

---

### 📈 性能监控看板

```javascript
// Vue 组件示例
<template>
  <div class="performance-dashboard">
    <div class="metric-card">
      <h3>LCP</h3>
      <div :class="lcpClass">{{ lcp }}ms</div>
      <div class="trend">{{ lcpTrend }}</div>
    </div>
    
    <div class="metric-card">
      <h3>FID</h3>
      <div :class="fidClass">{{ fid }}ms</div>
    </div>
    
    <div class="metric-card">
      <h3>CLS</h3>
      <div :class="clsClass">{{ cls }}</div>
    </div>
  </div>
</template>

<script setup>
import { ref, computed } from 'vue'

const lcp = ref(0)
const fid = ref(0)
const cls = ref(0)

// 评分样式
const lcpClass = computed(() => {
  if (lcp.value < 2500) return 'good'
  if (lcp.value < 4000) return 'needs-improvement'
  return 'poor'
})

// 获取性能数据
async function fetchMetrics() {
  const response = await fetch('/api/metrics')
  const data = await response.json()
  lcp.value = data.lcp
  fid.value = data.fid
  cls.value = data.cls
}

fetchMetrics()
</script>
```

---

## ✅ 学习检查清单

- [ ] 理解核心性能指标
- [ ] 掌握 Performance API 使用
- [ ] 理解 Web Vitals (LCP/FID/CLS)
- [ ] 会使用 Lighthouse 进行性能评分
- [ ] 了解 RUM 监控原理
- [ ] 会使用 Chrome DevTools 分析性能
- [ ] 能搭建性能监控系统

---

## 🚀 下一步

- [ ] 在项目中集成 Web Vitals 监控
- [ ] 使用 Lighthouse CI 集成到 CI/CD
- [ ] 搭建 RUM 监控系统
- [ ] 定期分析性能数据，持续优化

---

**没有测量，就没有优化！** 📊

