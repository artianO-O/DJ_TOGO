# 组件渲染流程

> 从创建到渲染，理解 Vue3 组件的完整生命周期

## 🔄 组件渲染的整体流程

```
创建应用 → 挂载根组件 → 递归渲染子组件 → 响应式更新
```

让我们一步步深入了解。

---

## 🎯 第一步：createApp 做了什么？

```javascript
import { createApp } from 'vue'
import App from './App.vue'

const app = createApp(App)
app.mount('#app')
```

### createApp 内部

```javascript
function createApp(rootComponent) {
  const app = {
    _component: rootComponent,  // 保存根组件
    _container: null,           // 挂载容器
    
    // 注册全局组件
    component(name, component) {
      // ...
    },
    
    // 注册全局指令
    directive(name, directive) {
      // ...
    },
    
    // 安装插件
    use(plugin) {
      // ...
    },
    
    // 挂载
    mount(containerOrSelector) {
      // 获取容器 DOM 元素
      const container = typeof containerOrSelector === 'string'
        ? document.querySelector(containerOrSelector)
        : containerOrSelector
      
      // 创建根组件的虚拟节点
      const vnode = createVNode(rootComponent)
      
      // 渲染到容器
      render(vnode, container)
      
      app._container = container
      return vnode.component.proxy  // 返回组件实例
    }
  }
  
  return app
}
```

---

## 🎯 第二步：组件的虚拟节点

### 组件 vs 元素

```javascript
// 普通元素的 VNode
const elementVNode = {
  type: 'div',
  props: { class: 'container' },
  children: 'Hello'
}

// 组件的 VNode
const componentVNode = {
  type: MyComponent,  // type 是一个组件对象
  props: { msg: 'Hello' },
  children: null
}
```

### 组件对象的结构

```javascript
// 一个 Vue 组件
const MyComponent = {
  name: 'MyComponent',
  
  // 组件的 props 定义
  props: {
    msg: String
  },
  
  // setup 函数
  setup(props, context) {
    const count = ref(0)
    return { count }
  },
  
  // 渲染函数（或模板编译后的结果）
  render() {
    return h('div', null, [
      h('p', null, this.msg),
      h('p', null, this.count)
    ])
  }
}
```

---

## 🎯 第三步：挂载组件

```javascript
function mountComponent(vnode, container) {
  // 1. 创建组件实例
  const instance = createComponentInstance(vnode)
  vnode.component = instance
  
  // 2. 设置组件实例（执行 setup、处理 props 等）
  setupComponent(instance)
  
  // 3. 设置渲染 effect
  setupRenderEffect(instance, container)
}
```

### 创建组件实例

```javascript
function createComponentInstance(vnode) {
  const instance = {
    vnode,
    type: vnode.type,          // 组件定义
    props: {},                  // 组件 props
    attrs: {},                  // 非 props 属性
    slots: {},                  // 插槽
    emit: null,                 // 事件发射
    proxy: null,                // 代理对象（this）
    ctx: {},                    // 上下文
    setupState: {},             // setup 返回值
    data: {},                   // data 数据
    isMounted: false,           // 是否已挂载
    subTree: null,              // 组件渲染的虚拟 DOM 树
    update: null,               // 更新函数
  }
  
  return instance
}
```

### 设置组件

```javascript
function setupComponent(instance) {
  const { props, children } = instance.vnode
  
  // 1. 处理 props
  initProps(instance, props)
  
  // 2. 处理 slots
  initSlots(instance, children)
  
  // 3. 执行 setup
  setupStatefulComponent(instance)
}

function setupStatefulComponent(instance) {
  const Component = instance.type
  
  // 创建代理对象（组件实例的 this）
  instance.proxy = new Proxy(instance.ctx, {
    get(target, key) {
      // 按优先级查找：setupState > data > props
      const { setupState, data, props } = instance
      
      if (key in setupState) {
        return setupState[key]
      } else if (key in data) {
        return data[key]
      } else if (key in props) {
        return props[key]
      }
    },
    set(target, key, value) {
      const { setupState, data } = instance
      
      if (key in setupState) {
        setupState[key] = value
        return true
      } else if (key in data) {
        data[key] = value
        return true
      }
      return false
    }
  })
  
  // 执行 setup 函数
  const { setup } = Component
  if (setup) {
    const setupResult = setup(instance.props, {
      attrs: instance.attrs,
      slots: instance.slots,
      emit: instance.emit
    })
    
    // setup 返回对象，作为组件状态
    if (typeof setupResult === 'object') {
      instance.setupState = proxyRefs(setupResult)
    }
    // setup 返回函数，作为 render 函数
    else if (typeof setupResult === 'function') {
      instance.render = setupResult
    }
  }
}
```

---

## 🎯 第四步：设置渲染 Effect

**关键点**：组件的渲染被包装成一个响应式 effect，当数据变化时自动重新渲染。

```javascript
function setupRenderEffect(instance, container) {
  // 组件更新函数
  const componentUpdateFn = () => {
    if (!instance.isMounted) {
      // ========== 首次挂载 ==========
      
      // 调用 render 函数，生成虚拟 DOM
      const subTree = instance.render.call(instance.proxy)
      instance.subTree = subTree
      
      // 渲染虚拟 DOM 到真实 DOM
      patch(null, subTree, container)
      
      // 标记已挂载
      instance.isMounted = true
      
      // 触发 onMounted 钩子
      if (instance.mounted) {
        instance.mounted.forEach(fn => fn())
      }
    } else {
      // ========== 更新 ==========
      
      // 获取新的虚拟 DOM
      const nextTree = instance.render.call(instance.proxy)
      const prevTree = instance.subTree
      instance.subTree = nextTree
      
      // Diff 并更新
      patch(prevTree, nextTree, container)
    }
  }
  
  // 创建响应式 effect
  const effect = new ReactiveEffect(componentUpdateFn, () => {
    // 调度器：把更新任务放入队列，异步批量执行
    queueJob(instance.update)
  })
  
  instance.update = effect.run.bind(effect)
  
  // 首次执行，触发挂载
  instance.update()
}
```

---

## 🔄 完整流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                    Vue3 组件渲染流程                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. createApp(App)                                              │
│  ┌─────────────────────────────────┐                           │
│  │ 创建应用实例                    │                           │
│  │ 返回 { mount, use, component }  │                           │
│  └─────────────────────────────────┘                           │
│                    │                                            │
│                    ▼                                            │
│  2. app.mount('#app')                                           │
│  ┌─────────────────────────────────┐                           │
│  │ 创建根组件的 VNode              │                           │
│  │ 调用 render(vnode, container)   │                           │
│  └─────────────────────────────────┘                           │
│                    │                                            │
│                    ▼                                            │
│  3. mountComponent()                                            │
│  ┌─────────────────────────────────┐                           │
│  │ createComponentInstance()       │ → 创建组件实例             │
│  │ setupComponent()                │ → 处理 props/slots/setup  │
│  │ setupRenderEffect()             │ → 创建响应式渲染          │
│  └─────────────────────────────────┘                           │
│                    │                                            │
│                    ▼                                            │
│  4. 首次渲染                                                    │
│  ┌─────────────────────────────────┐                           │
│  │ render() 生成 VNode 树          │                           │
│  │ patch(null, vnode, container)   │                           │
│  │ 递归创建 DOM / 挂载子组件       │                           │
│  └─────────────────────────────────┘                           │
│                    │                                            │
│                    ▼                                            │
│  5. 响应式更新                                                  │
│  ┌─────────────────────────────────┐                           │
│  │ 数据变化 → trigger()            │                           │
│  │ 调度器 → queueJob()             │                           │
│  │ 异步批量执行更新                │                           │
│  └─────────────────────────────────┘                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔍 生命周期钩子的执行时机

```javascript
// 组件实例
const instance = {
  // 生命周期钩子存储
  bc: [],   // beforeCreate (Options API)
  c: [],    // created (Options API)
  bm: [],   // beforeMount
  m: [],    // mounted
  bu: [],   // beforeUpdate
  u: [],    // updated
  bum: [],  // beforeUnmount
  um: [],   // unmounted
}

// 在 setup 中注册钩子
function onMounted(fn) {
  // 获取当前正在执行 setup 的组件实例
  const instance = getCurrentInstance()
  if (instance) {
    instance.m.push(fn)
  }
}
```

### 生命周期执行顺序

```
┌─────────────────────────────────────────────────┐
│                 组件生命周期                     │
├─────────────────────────────────────────────────┤
│                                                 │
│  创建阶段                                       │
│  ────────────────────────                       │
│  setup()                  ← 在这里执行 setup    │
│  onBeforeMount()          ← DOM 挂载前          │
│                                                 │
│  挂载阶段                                       │
│  ────────────────────────                       │
│  render()                 ← 生成 VNode          │
│  patch() / mount()        ← 创建真实 DOM        │
│  onMounted()              ← DOM 已挂载          │
│                                                 │
│  更新阶段                                       │
│  ────────────────────────                       │
│  数据变化                                       │
│  onBeforeUpdate()         ← 更新前              │
│  render()                 ← 重新生成 VNode      │
│  patch()                  ← Diff 更新           │
│  onUpdated()              ← 更新完成            │
│                                                 │
│  卸载阶段                                       │
│  ────────────────────────                       │
│  onBeforeUnmount()        ← 卸载前              │
│  unmount()                ← 移除 DOM            │
│  onUnmounted()            ← 卸载完成            │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 💡 父子组件的渲染顺序

```vue
<!-- Parent.vue -->
<template>
  <div>
    <Child />
  </div>
</template>

<!-- Child.vue -->
<template>
  <div>Child</div>
</template>
```

**挂载顺序**：
```
Parent setup
Parent onBeforeMount
  Child setup
  Child onBeforeMount
  Child onMounted    ← 子组件先 mounted
Parent onMounted     ← 父组件后 mounted
```

**更新顺序**：
```
Parent onBeforeUpdate
  Child onBeforeUpdate
  Child onUpdated
Parent onUpdated
```

**卸载顺序**：
```
Parent onBeforeUnmount
  Child onBeforeUnmount
  Child onUnmounted
Parent onUnmounted
```

---

## 🚀 异步更新与 nextTick

### 为什么需要异步更新？

```javascript
const count = ref(0)

// 同步修改多次
count.value = 1
count.value = 2
count.value = 3

// 如果每次修改都立即渲染，会渲染 3 次！
// 异步批量更新：只在最后渲染 1 次
```

### 更新队列

```javascript
const queue = []       // 更新任务队列
let isFlushing = false // 是否正在执行

function queueJob(job) {
  // 去重：同一个组件的更新只加入一次
  if (!queue.includes(job)) {
    queue.push(job)
  }
  
  // 异步执行
  if (!isFlushing) {
    isFlushing = true
    Promise.resolve().then(() => {
      // 执行所有更新任务
      queue.forEach(job => job())
      queue.length = 0
      isFlushing = false
    })
  }
}
```

### nextTick 的实现

```javascript
function nextTick(fn) {
  return fn ? Promise.resolve().then(fn) : Promise.resolve()
}

// 使用
const count = ref(0)
count.value = 100

// 此时 DOM 还没更新
console.log(document.querySelector('#count').textContent) // 0

await nextTick()

// 现在 DOM 已更新
console.log(document.querySelector('#count').textContent) // 100
```

---

## 📝 总结

| 阶段 | 关键操作 |
|-----|---------|
| **创建应用** | createApp 返回应用实例 |
| **挂载组件** | 创建实例 → setup → 渲染 effect |
| **首次渲染** | render → patch → 创建 DOM |
| **响应式更新** | 数据变化 → queueJob → 批量更新 |
| **Diff 更新** | patch → 最小化 DOM 操作 |
| **异步更新** | Promise.resolve → 批量执行 |

### 核心概念

1. **组件 = 数据 + 渲染函数**
2. **渲染 = effect**（响应式追踪）
3. **更新 = 异步批量**（性能优化）
4. **DOM 操作 = Diff**（最小更新）

---

## 🚀 下一步

- [07-模板编译原理](./07-模板编译原理.md)



